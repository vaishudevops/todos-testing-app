const assert = require("assert");
const { isTypedArray } = require("util/types");
const{ div } = require("../src/maths");


describe("Test Math file",()=>{
    it("should return o",()=>{
        const n1 = (0);
        assert.equal(n1,0 , "Result should be 0")
    })
})